int fib(int n) {
    if (n <= 2)
        return 1;
    else
        if (n>2)
            return fib(n-1) + fib(n-2);
        else while (1) {
            print("Hi");
        } }
}

int main() {
    int n, x;
    x=getNum();
    x = 1.24+2-0.56+x;
    x = -x;
    x = fun(fun(x));
    n=(x*x*x*x*x+2%x*x*x+x)/(x*x*x*x+2*x*x+1);
    printf("fib: %d",fib(n));
    return 0;
}
